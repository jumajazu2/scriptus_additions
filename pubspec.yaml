name: scriptus
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  clipboard: ^0.1.3
  country_flags: ^3.0.0
  csv: ^6.0.0
  cupertino_icons: ^1.0.6
  diacritic: ^0.1.4
  dio: ^5.3.3
  dropdown_search: ^5.0.6
  file_picker: ^8.0.7
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.3
  freezed_annotation: ^2.2.0
  google_fonts: ^6.1.0
  hooks_riverpod: ^2.4.5
  intl: ^0.19.0
  json_annotation: ^4.8.1
  just_audio: ^0.9.39
  path: ^1.8.3
  path_provider: ^2.1.1
  riverpod_annotation: ^2.3.0
  sqflite: ^2.3.0
  tuple: ^2.0.1
  google_generative_ai: ^0.4.6
  just_audio_windows: ^0.2.2
  docx_template: ^0.4.0
  process_run: ^1.2.2
  mp3_info: ^0.2.1
  id3tag: ^0.2.0
  sqflite_common_ffi: ^2.3.4

dev_dependencies:
  build_runner: 2.4.13
  custom_lint: 0.6.4
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  riverpod_generator: ^2.3.11
  riverpod_lint: ^2.3.9
  

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/2023-03-18-1984-12-29-1000-Krefeld-english.csv
    - assets/db/MENG.SQLite3
    - assets/db/bible/mng-de.SQLite3
    - assets/db/bible/mng-ro.SQLite3
    - assets/db/bible/mng-hu.SQLite3
    - assets/db/versei.sqlite
    - assets/template_de.docx
    - .dart_tool/sqflite_common_ffi/databases/mng-de.SQLite3
    
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
